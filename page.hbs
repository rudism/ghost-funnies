{{!< default}}
{{#post}}
<div class="page-wrapper">
    <header class="post-header">
        <h3>{{title}}</h3>
        <time datetime="{{date published_at format="YYYY-MM-DDTHH:mm:ss"}}">Published {{date published_at format="MMMM Do YYYY"}}</time>
    </header>
    <div class="post-content">
        {{{content}}}
    </div>
    {{#if tags}}
        <div class="tags">{{tags}}</div>
    {{/if}}
</div>
{{/post}}
